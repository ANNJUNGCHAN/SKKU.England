### DB 분석
```
이제, db에 있는 source 엑셀을 분석한 후, 분석 결과를 REPORT.md 파일에 만들어줘. 해당 파일은 db폴더 내에 존재해야 해    
```

### 팀 표준 확인
```
CLAUDE.md, .claude/skills/, .claude/agents/ 폴더를 확인하고
팀 표준이 제대로 설정되어 있는지 검증해줘.
빠진 항목이 있으면 알려줘.
```

### 파이썬 가상환경 셋팅
- python -m venv env : 해당 설정을 먼저 실행
```
CLAUDE.md에 아래의 사항을 추가해줘
- 너는 env 파이썬 가상환경만을 사용하여 개발할거야
- 이 때, 너가 설치하는 패키지들은 requirements.txt 파일에 모두 작성해줘
- 프로그램 설치도 env 가상환경에서 실행해야하고, 모든 파이썬은 해당 가상환경을 사용해야해
```

### 코드 관리 서브 에이전트 만들기
```
코드 관리 서브 에이전트를 만들어줘
.claude/agents/code-control.md 파일로 만들어줘.

해당 에이전트는 한 단위의 작업이 실행될때마다 git에 매우 상세한 커밋메세지를 남기고
해당 디렉토레 내에 있는 git에 push하는 기능을 수행해
이 때, commit 하기 전 최종으로 코드리뷰를 하고, 수정할 사항을 반영해 push하고
각각의 코드에 주석도 매우 꼼꼼히 달아줘
모든 주석과 커밋 메시지는 한국어로 작성해
```


### 웹검색 서브 에이전트 만들기
```
웹검색 전용 서브에이전트를 만들어줘.
.claude/agents/web-search.md 파일로 만들어줘.
```

### 배경지식 에이전트 만들기
```
배경지식 전용 서브에이전트를 만들어줘.
.claude/agents/background-search.md 파일로 만들어줘.

- 배경지식 전용 서브에이전트란, background 내 폴더들에 있는 파일에 대한 내용을 인지한 후, 해당 정보들을 사용하는 에이전트야
- 이 때, 배경지식 전용 에이전트는 폴더 안에 있는 배경지식들을 최대한 활용하여 답변하거나, 여러 작업들을 수행해야 해
```

### 에이전트 팀 확인
```
CLAUDE.md, .claude/skills/, .claude/agents/ 폴더를 확인하고
팀 표준이 제대로 설정되어 있는지 검증해줘.
빠진 항목이 있으면 알려줘.
```

### 체크리스트 도입
```
체크리스트르 만들고, checklist.md에 저장해
checklist.md를 보고, 해당 작업을 하나씩 진행해.   
```

### 작업계획서 harness
```
아래 내용을 바탕으로 작업 계획서를 다시 작성해줘

작업 계획서 내 아래의 사항을 무조건 지켜줘
- 파일명, 경로명 명시 금지
- SQL문 사용 금지
- 스크립트 사용 금지
- 코드 사용 금지
- 디렉토리 구조 사용 금지
- 체크리스트 언급 금지
```

### 작업계획서 TO-DO
```
PLAN.md를 아래와 같이 수정합니다.
- REPORT.md를 잘 읽어보고, 해당 내용을 최대한 반영
- web-search 툴을 사용해서 한국은행 ECOS 시스템을 파악한 후, 반영
- xlsx로 데이터를 저장하는 것이 아닌, 무조건 csv 형태로 파일을 저장
- 하나의 csv 내에는 꼭 하나의 시트만 있어야함. 복수 시트 생성은 금지
```

```
체크리스트대로 업무를 수행해(db/CHECKLIST.md)
이 때, 각각의 체크 항목에 대해서 완료될때마다 code-control를 무조건 수행해야 해
```